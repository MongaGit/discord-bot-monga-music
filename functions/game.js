const lib = require('lib')({token: process.env.STDLIB_SECRET_TOKEN});
//Post whatever code you want the bot to run after both users are in the game
// ${id}_Player1 and ${id}_Player2 are player util keys to retrieve the player ids, use them as you wish, remember, there is no payload for this event. 
